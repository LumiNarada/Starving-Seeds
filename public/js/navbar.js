function intro() {
    let element = document.getElementById("intro");
    let y = element.getBoundingClientRect().top + window.pageYOffset -33;
    window.scrollTo({top: y, behavior: 'smooth'});
}
function autumn() {
    let element = document.getElementById("Autumn");
    let y = element.getBoundingClientRect().top + window.pageYOffset -33;
    window.scrollTo({top: y, behavior: 'smooth'});
}
function winter() {
    let element = document.getElementById("Winter");
    let y = element.getBoundingClientRect().top + window.pageYOffset -33;
    window.scrollTo({top: y, behavior: 'smooth'});
}
function spring() {
    let element = document.getElementById("Spring");
    let y = element.getBoundingClientRect().top + window.pageYOffset -33;
    window.scrollTo({top: y, behavior: 'smooth'});
}
function summer() {
    let element = document.getElementById("Summer");
    let y = element.getBoundingClientRect().top + window.pageYOffset -33;
    window.scrollTo({top: y, behavior: 'smooth'});
}



